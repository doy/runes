#ifndef _RUNES_H
#define _RUNES_H

#include <cairo.h>
#include <uv.h>

struct runes_term;
struct runes_window;

typedef struct runes_term RunesTerm;
typedef struct runes_window RunesWindow;

#include "term.h"
#include "display.h"

#include "xlib.h"

#endif
